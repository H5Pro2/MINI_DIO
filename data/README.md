# Data

Controlled external worlds for MINI_DIO live here.

Current CSV expectation:

```text
timestamp_ms,open,high,low,close,volume
```

Large source datasets should stay outside git unless deliberately added. The normal workflow is to copy or generate focused controlled worlds into this folder.

