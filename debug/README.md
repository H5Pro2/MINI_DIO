# Debug

Run outputs and report artifacts are written here.

This folder is intentionally ignored by git except for this README. Debug artifacts can be useful locally, but they should not become source files unless a specific result is promoted into `docs/befunde/`.

