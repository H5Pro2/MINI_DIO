# Reports

This folder contains passive diagnostic scripts copied from the Mini-DIO research chain.

They are research readers, not runtime logic:

- no entries,
- no gates,
- no motor decisions,
- no trading rules.

Several reports expect already generated CSV/JSON artifacts in `debug/`. That is intentional for now: these scripts are used to inspect passive MCM field organization after runs, not to drive Mini-DIO.

